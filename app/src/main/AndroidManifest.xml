<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shakibaenur.tenminuteschool">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ten_minute"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ten_minute_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.document.DocumentActivity"
            android:screenOrientation="portrait"></activity>
        <activity android:name=".ui.video.VideoActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".ui.home.HomeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>